cmake_minimum_required(VERSION 3.25.2)

project(cache)

add_executable(${PROJECT_NAME} main.cpp)

# add_subdirectory(external/tbb)

# target_include_directories(${PROJECT_NAME} PUBLIC external/tbb/include)

# target_link_directories(${PROJECT_NAME} PUBLIC external/tbb/src)

